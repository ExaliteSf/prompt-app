[build]
  command = "npm run build"
  publish = "dist"

# Autoriser login.html comme page directe
[[redirects]]
  from = "/login"
  to = "/login.html"
  status = 200

# SPA fallback — NE PAS supprimer
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
